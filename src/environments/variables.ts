export class Variables {
	static baseUrl = 'http://localhost:8080'; // CORS issue for omitting http://
	//static baseUrl='static base_url="http://<static_IP>:8080";' // Useful for configuration check.
}
